apply from: 'gradle/versions.gradle'

allure {
    version = allureVersion

    adapter {
        allureJavaVersion = allureVersion
        frameworks {
            junit5 {
                enabled = true
            }
        }
    }
}
